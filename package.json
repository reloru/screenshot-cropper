{
  "name": "screenshot-cropper",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Measure the blank voids around a phone screenshot and crop them off — entirely in the browser.",
  "repository": {
    "type": "git",
    "url": "https://github.com/reloru/screenshot-cropper.git"
  },
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test 'test/**/*.test.mjs'",
    "test:e2e": "node scripts/test-e2e.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.114.0"
  }
}
